#include <stdio.h>  // printf, perror
#include <unistd.h> // exit
#include <fcntl.h>  // fork, execlp, close

int main(void)
{
    // Syscalls needed:
    // fork()
    // exec()
    // pipe()
    // dup2()
}